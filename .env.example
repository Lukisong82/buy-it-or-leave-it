# Copy to .env and replace with your TypeSafe key. Never put this in public/.
TYPESAFE_API_KEY=replace-with-your-key
PORT=3000
# Optional: pin a Jev model instead of the default alias.
TYPESAFE_MODEL=jev-latest
